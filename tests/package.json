{
  "name": "crosstech-site-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "cd .. && npx firebase emulators:exec --only firestore --project demo-crosstech \"node tests/firestore-rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.35.1"
  }
}
